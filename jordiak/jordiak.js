jordiak.onclick = function () {
    let bulan = document.querySelector("#bulan").value;
    let tanggal = document.querySelector("#tanggal").value;
    let jordiak = "";
    if (bulan == 1 && tanggal >= 1 || tanggal <= 19) {
        jordiak = "capricornus";
    }
    document.querySelector("#hasil").innerHTML = jordiak;
}

jordiak.onclick = function () {
    let bulan = document.querySelector("#bulan").value;
    let tanggal = document.querySelector("#tanggal").value;
    let jordiak = "";
    if (bulan == 1 && tanggal >= 20 || tanggal <= 31 || bulan == 2 && tanggal >= 1 || tanggal <= 18) {
        jordiak = "aquarius";
    }
    document.querySelector("#hasil").innerHTML = jordiak;
}

jordiak.onclick = function () {
    let bulan = document.querySelector("#bulan").value;
    let tanggal = document.querySelector("#tanggal").value;
    let jordiak = "";
    if (bulan == 2 && tanggal >= 19 || tanggal <= 30 || bulan == 3 && tanggal >= 1 || tanggal <= 20) {
        jordiak = "pisces";
    }
    document.querySelector("#hasil").innerHTML = jordiak;
}

jordiak.onclick = function () {
    let bulan = document.querySelector("#bulan").value;
    let tanggal = document.querySelector("#tanggal").value;
    let jordiak = "";
    if (bulan == 3 && tanggal >= 21 || tanggal <= 31 || bulan == 4 && tanggal >= 1 || tanggal <= 19) {
        jordiak = "aries";
    }
    document.querySelector("#hasil").innerHTML = jordiak;
}

jordiak.onclick = function () {
    let bulan = document.querySelector("#bulan").value;
    let tanggal = document.querySelector("#tanggal").value;
    let jordiak = "";
    if (bulan == 4 && tanggal >= 20 || tanggal <= 30 || bulan == 5 && tanggal >= 1 || tanggal <= 20) {
        jordiak = "taurus";
    }
    document.querySelector("#hasil").innerHTML = jordiak;
}

jordiak.onclick = function () {
    let bulan = document.querySelector("#bulan").value;
    let tanggal = document.querySelector("#tanggal").value;
    let jordiak = "";
    if (bulan == 5 && tanggal >= 21 || tanggal <= 31 || bulan == 6 && tanggal >= 1 || tanggal <= 20) {
        jordiak = "gemini";
    }
    document.querySelector("#hasil").innerHTML = jordiak;
}

jordiak.onclick = function () {
    let bulan = document.querySelector("#bulan").value;
    let tanggal = document.querySelector("#tanggal").value;
    let jordiak = "";
    if (bulan == 6 && tanggal >= 21 || tanggal <= 30 || bulan == 7 && tanggal >= 1 || tanggal <= 22) {
        jordiak = "kangker";
    }
    document.querySelector("#hasil").innerHTML = jordiak;
}

jordiak.onclick = function () {
    let bulan = document.querySelector("#bulan").value;
    let tanggal = document.querySelector("#tanggal").value;
    let jordiak = "";
    if (bulan == 7 && tanggal >= 23 || tanggal <= 31 || bulan == 8 && tanggal >= 1 || tanggal <= 22) {
        jordiak = "leo";
    }
    document.querySelector("#hasil").innerHTML = jordiak;
}

jordiak.onclick = function () {
    let bulan = document.querySelector("#bulan").value;
    let tanggal = document.querySelector("#tanggal").value;
    let jordiak = "";
    if (bulan == 8 && tanggal >= 23 || tanggal <= 31 || bulan == 9 && tanggal >= 1 || tanggal <= 22) {
        jordiak = "virgo";
    }
    document.querySelector("#hasil").innerHTML = jordiak;
}

jordiak.onclick = function () {
    let bulan = document.querySelector("#bulan").value;
    let tanggal = document.querySelector("#tanggal").value;
    let jordiak = "";
    if (bulan == 9 && tanggal >= 23 || tanggal <= 30 || bulan == 10 && tanggal >= 1 || tanggal <= 22) {
        jordiak = "libra";
    }
    document.querySelector("#hasil").innerHTML = jordiak;
}

jordiak.onclick = function () {
    let bulan = document.querySelector("#bulan").value;
    let tanggal = document.querySelector("#tanggal").value;
    let jordiak = "";
    if (bulan == 10 && tanggal >= 23 || tanggal <= 31 || bulan == 11 && tanggal >= 1 || tanggal <= 21) {
        jordiak = "scorpion";
    }
    document.querySelector("#hasil").innerHTML = jordiak;
}

jordiak.onclick = function () {
    let bulan = document.querySelector("#bulan").value;
    let tanggal = document.querySelector("#tanggal").value;
    let jordiak = "";
    if (bulan == 11 && tanggal >= 22 || tanggal <= 31 || bulan == 12 && tanggal >= 1 || tanggal <= 21) {
        jordiak = "sagitta";
    }
    document.querySelector("#hasil").innerHTML = jordiak;
}

jordiak.onclick = function () {
    let bulan = document.querySelector("#bulan").value;
    let tanggal = document.querySelector("#tanggal").value;
    let jordiak = "";
    if (bulan == 12 && tanggal >= 22  || tanggal <= 31) {
        jordiak = "capricon";
    }
    document.querySelector("#hasil").innerHTML = jordiak;
}